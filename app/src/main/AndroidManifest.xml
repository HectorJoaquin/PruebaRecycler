<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.sqliteprueba003">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.RecyclerViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.RegistrarUsuarioActivity" />
        <activity android:name=".Activitys.ConsultarUsuarioActivity" />
        <activity android:name=".Activitys.SpinnerConsultaActivity" />
        <activity android:name=".Activitys.ListViewConsultaActivity" />
        <activity android:name=".Activitys.DetalleUsuarioActivity" />
        <activity android:name=".Activitys.RegistrarMascotaActivity" />
        <activity android:name=".PruebaActualizarActivity"></activity>
    </application>

</manifest>